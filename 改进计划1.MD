# ResumePoint 改进计划 1

> 基于美国市场深度分析，针对UI/UX、功能模块、代码质量进行全面优化

---

## 目录

1. [改进概述](#改进概述)
2. [改进原则](#改进原则)
3. [模块一：Onboarding引导流程](#模块一onboarding引导流程)
4. [模块二：ProgressCard组件增强](#模块二progresscard组件增强)
5. [模块三：智能提醒系统](#模块三智能提醒系统)
6. [模块四：平台图标系统](#模块四平台图标系统)
7. [模块五：数据导出增强](#模块五数据导出增强)
8. [模块六：设置页面优化](#模块六设置页面优化)
9. [代码清理计划](#代码清理计划)
10. [测试验证标准](#测试验证标准)
11. [实施时间表](#实施时间表)

---

## 改进概述

### 改进背景

基于美国市场深度分析，发现以下核心问题：

| 问题类别 | 具体问题 | 影响程度 | 优先级 |
|---------|---------|---------|--------|
| 用户体验 | 缺少Onboarding引导 | 高 | P0 |
| UI设计 | 进度卡片信息密度不足 | 中 | P1 |
| 功能缺失 | 智能提醒不够智能 | 中 | P1 |
| 视觉一致性 | 平台图标使用SF Symbols | 低 | P2 |
| 功能缺失 | 数据导出格式单一 | 低 | P2 |
| 设置页面 | 缺少Pro功能入口 | 中 | P1 |

### 改进目标

1. **提升用户激活率** - 从预估50%提升至70%
2. **增强用户粘性** - D7留存从预估30%提升至40%
3. **提高付费转化** - 从预估1%提升至3%
4. **保持代码质量** - 遵循单一职责、高内聚低耦合原则

### 文档修复记录 (v1.1)

本版本修复了以下问题：

| 问题 | 修复内容 |
|------|---------|
| Onboarding组件分离 | 将 `OnboardingPageView` 和 `OnboardingProgressDots` 分离到单独文件 |
| ProgressCard代码复用 | 使用现有的 `CardStyle` modifier，遵循代码复用原则 |
| 测试用例依赖 | 将 `PersistenceController.preview` 替换为 `DependencyContainer.preview` |
| DependencyContainer集成 | 添加完整的 `DependencyContainer` 修改说明 |
| SettingsViewModel集成 | 添加 `reminderService` 属性和相关方法说明 |
| ExportService集成 | 添加 `ProgressStorageService` 的导出方法集成说明 |
| 文件清单完善 | 添加修改文件清单和新增文件清单 |

---

## 改进原则

### 原则1：需求描述与检验标准

每个改进模块必须包含：

```markdown
## 需求描述
- 用户故事
- 功能目标
- 验收标准

## 生成规则
- 代码规范
- 命名约定
- 文件结构

## 检验标准
- 单元测试用例
- UI测试用例
- 代码审查清单
```

### 原则2：功能模块原则

```
单一职责：一个文件/类只做一件事
高内聚低耦合：模块内部紧密相关，模块间松散依赖
命名清晰：见名知意，遵循Swift命名规范
文件结构语义化：目录结构反映功能层次
```

### 原则3：代码复用原则

```
优先复用现有模块
抽象遵循"三次法则"：遇到三次再抽
最小代码原则：能用一行不用两行
```

### 原则4：代码清理原则

```
重构清理流程：
1. 标记废弃（@available(*, deprecated)）
2. 验证无影响（测试通过）
3. 删除代码
4. 提交信息注明清理范围
```

### 原则5：风格一致性原则

```
UI风格：延续现有Liquid Glass设计
代码风格：遵循现有项目规范
功能风格：与现有功能保持一致
```

### 原则6：继承兼容性原则

```
优先继承现有协议和类
避免硬编码，使用配置和常量
保持向后兼容
```

---

## 模块一：Onboarding引导流程

### 1.1 需求描述

**用户故事：**
> 作为新用户，我希望在首次打开App时了解如何使用，以便快速上手

**功能目标：**
- 展示App核心价值
- 引导用户完成首次操作
- 收集必要权限

**验收标准：**
- [ ] 首次启动显示Onboarding
- [ ] 完成后不再显示
- [ ] 支持跳过
- [ ] 支持"稍后查看"

### 1.2 生成规则

#### 文件结构

```
Features/
└── Onboarding/
    ├── OnboardingView.swift           # 主视图
    ├── OnboardingViewModel.swift      # 视图模型
    ├── OnboardingPage.swift           # 单页模型
    └── Components/
        ├── OnboardingPageView.swift   # 单页视图组件
        └── OnboardingProgressDots.swift # 进度指示器
```

#### 命名约定

```swift
// 命名模式：Onboarding + 功能描述
// 例如：
// - OnboardingView
// - OnboardingPage
// - OnboardingPageView

// 状态命名：is + 形容词
// 例如：
// - isCompleted
// - isSkipped
// - isPresented
```

#### 代码规范

```swift
// 遵循现有ViewModel模式
@MainActor
final class OnboardingViewModel: ObservableObject {
    @Published var currentPage: Int = 0
    @Published var isCompleted: Bool = false
    
    // 使用Constants中的常量
    let pages: [OnboardingPage] = [
        .init(
            icon: "play.rectangle.fill",
            title: "Welcome to ResumePoint",
            subtitle: "Never lose your streaming progress again",
            color: .accentColor
        ),
        // ...
    ]
}
```

### 1.3 代码实现

#### OnboardingPage.swift

```swift
import SwiftUI

struct OnboardingPage: Identifiable {
    let id = UUID()
    let icon: String
    let title: String
    let subtitle: String
    let color: Color
    
    static let welcome: OnboardingPage = .init(
        icon: "play.rectangle.fill",
        title: "Welcome to ResumePoint",
        subtitle: "Never lose your streaming progress again",
        color: .accentColor
    )
    
    static let autoDetect: OnboardingPage = .init(
        icon: "waveform.badge.magnifyingglass",
        title: "Auto-Detect Playback",
        subtitle: "We automatically track your streaming progress across all platforms",
        color: .blue
    )
    
    static let manual: OnboardingPage = .init(
        icon: "hand.tap.fill",
        title: "Manual Entry",
        subtitle: "Can also add progress manually for any video",
        color: .purple
    )
    
    static let sync: OnboardingPage = .init(
        icon: "icloud.fill",
        title: "iCloud Sync",
        subtitle: "Your progress syncs across all your devices",
        color: .cyan
    )
    
    static let all: [OnboardingPage] = [.welcome, .autoDetect, .manual, .sync]
}
```

#### OnboardingViewModel.swift

```swift
import SwiftUI

@MainActor
final class OnboardingViewModel: ObservableObject {
    @Published var currentPage: Int = 0
    @Published var isCompleted: Bool = false
    
    let pages: [OnboardingPage] = OnboardingPage.all
    
    var currentPageData: OnboardingPage {
        pages[currentPage]
    }
    
    var isLastPage: Bool {
        currentPage == pages.count - 1
    }
    
    var isFirstPage: Bool {
        currentPage == 0
    }
    
    func nextPage() {
        if isLastPage {
            completeOnboarding()
        } else {
            withAnimation(.spring(response: 0.3, dampingFraction: 0.8)) {
                currentPage += 1
            }
        }
    }
    
    func previousPage() {
        guard !isFirstPage else { return }
        withAnimation(.spring(response: 0.3, dampingFraction: 0.8)) {
            currentPage -= 1
        }
    }
    
    func skip() {
        completeOnboarding()
    }
    
    private func completeOnboarding() {
        UserDefaults.standard.set(true, forKey: Constants.Storage.onboardingCompletedKey)
        withAnimation {
            isCompleted = true
        }
    }
    
    static var shouldShowOnboarding: Bool {
        !UserDefaults.standard.bool(forKey: Constants.Storage.onboardingCompletedKey)
    }
}
```

#### OnboardingView.swift

```swift
import SwiftUI

struct OnboardingView: View {
    @StateObject private var viewModel = OnboardingViewModel()
    @Environment(\.dismiss) private var dismiss
    
    var body: some View {
        VStack(spacing: 0) {
            skipButton
            pageContent
            bottomControls
        }
        .background(Color(.systemBackground))
        .onChange(of: viewModel.isCompleted) { _, completed in
            if completed {
                dismiss()
            }
        }
    }
    
    private var skipButton: some View {
        HStack {
            Spacer()
            Button("Skip") {
                viewModel.skip()
            }
            .font(.subheadline.weight(.medium))
            .foregroundStyle(.secondary)
            .padding()
        }
    }
    
    private var pageContent: some View {
        TabView(selection: $viewModel.currentPage) {
            ForEach(Array(viewModel.pages.enumerated()), id: \.element.id) { index, page in
                OnboardingPageView(page: page)
                    .tag(index)
            }
        }
        .tabViewStyle(.page(indexDisplayMode: .never))
    }
    
    private var bottomControls: some View {
        VStack(spacing: Constants.UI.standardPadding) {
            OnboardingProgressDots(
                total: viewModel.pages.count,
                current: viewModel.currentPage
            )
            
            Button(action: { viewModel.nextPage() }) {
                Text(viewModel.isLastPage ? "Get Started" : "Continue")
                    .font(.headline)
                    .foregroundStyle(.white)
                    .frame(maxWidth: .infinity)
                    .padding(.vertical, 16)
                    .background(viewModel.currentPageData.color)
                    .clipShape(RoundedRectangle(cornerRadius: Constants.UI.compactCornerRadius))
            }
            .padding(.horizontal, Constants.UI.standardPadding)
        }
        .padding(.bottom, Constants.UI.standardPadding * 2)
    }
}
```

#### Components/OnboardingPageView.swift

```swift
import SwiftUI

struct OnboardingPageView: View {
    let page: OnboardingPage
    
    var body: some View {
        VStack(spacing: 32) {
            Image(systemName: page.icon)
                .font(.system(size: 80))
                .foregroundStyle(page.color)
            
            VStack(spacing: 12) {
                Text(page.title)
                    .font(.title.weight(.bold))
                    .multilineTextAlignment(.center)
                
                Text(page.subtitle)
                    .font(.body)
                    .foregroundStyle(.secondary)
                    .multilineTextAlignment(.center)
                    .padding(.horizontal, 40)
            }
        }
    }
}
```

#### Components/OnboardingProgressDots.swift

```swift
import SwiftUI

struct OnboardingProgressDots: View {
    let total: Int
    let current: Int
    
    var body: some View {
        HStack(spacing: 8) {
            ForEach(0..<total, id: \.self) { index in
                Circle()
                    .fill(index == current ? Color.accentColor : Color.gray.opacity(0.3))
                    .frame(width: 8, height: 8)
                    .animation(.spring(response: 0.3), value: current)
            }
        }
    }
}
```

### 1.4 集成到现有代码

#### 修改 ResumePointApp.swift

```swift
import SwiftUI

@main
struct ResumePointApp: App {
    @StateObject private var dependencies = DependencyContainer()
    @State private var showOnboarding = OnboardingViewModel.shouldShowOnboarding
    
    var body: some Scene {
        WindowGroup {
            ContentView(dependencies: dependencies)
                .fullScreenCover(isPresented: $showOnboarding) {
                    OnboardingView()
                }
        }
    }
}
```

#### 修改 Constants.swift

```swift
enum Constants {
    // ... 现有代码 ...
    
    enum Storage {
        static let freeTierLimit = 50
        static let maxTitleLength = 200
        static let maxNotesLength = 1000
        static let onboardingCompletedKey = "onboardingCompleted"  // 新增
    }
}
```

### 1.5 测试用例

#### 单元测试

```swift
import XCTest
@testable import ResumePoint

final class OnboardingViewModelTests: XCTestCase {
    
    var sut: OnboardingViewModel!
    
    override func setUp() {
        super.setUp()
        sut = OnboardingViewModel()
        UserDefaults.standard.removeObject(forKey: Constants.Storage.onboardingCompletedKey)
    }
    
    override func tearDown() {
        sut = nil
        super.tearDown()
    }
    
    func testInitialPage() {
        XCTAssertEqual(sut.currentPage, 0)
        XCTAssertFalse(sut.isCompleted)
        XCTAssertTrue(sut.isFirstPage)
    }
    
    func testNextPage() {
        sut.nextPage()
        XCTAssertEqual(sut.currentPage, 1)
        XCTAssertFalse(sut.isLastPage)
    }
    
    func testCompleteOnboarding() {
        let expectation = expectation(description: "Complete")
        
        sut.$isCompleted
            .dropFirst()
            .sink { completed in
                if completed {
                    expectation.fulfill()
                }
            }
        
        for _ in 0..<sut.pages.count {
            sut.nextPage()
        }
        
        wait(for: [expectation], timeout: 1.0)
        XCTAssertTrue(UserDefaults.standard.bool(forKey: Constants.Storage.onboardingCompletedKey))
    }
    
    func testSkipOnboarding() {
        sut.skip()
        XCTAssertTrue(sut.isCompleted)
        XCTAssertTrue(UserDefaults.standard.bool(forKey: Constants.Storage.onboardingCompletedKey))
    }
    
    func testShouldShowOnboarding() {
        UserDefaults.standard.removeObject(forKey: Constants.Storage.onboardingCompletedKey)
        XCTAssertTrue(OnboardingViewModel.shouldShowOnboarding)
        
        UserDefaults.standard.set(true, forKey: Constants.Storage.onboardingCompletedKey)
        XCTAssertFalse(OnboardingViewModel.shouldShowOnboarding)
    }
}
```

#### UI测试

```swift
import XCTest

final class OnboardingUITests: XCTestCase {
    
    var app: XCUIApplication!
    
    override func setUp() {
        continueAfterFailure = false
        app = XCUIApplication()
        app.launchArguments = ["--uitesting"]
        app.launch()
    }
    
    func testOnboardingFlow() {
        let skipButton = app.buttons["Skip"]
        XCTAssertTrue(skipButton.exists)
        
        let continueButton = app.buttons["Continue"]
        XCTAssertTrue(continueButton.exists)
        
        continueButton.tap()
        
        let page2Indicator = app.otherElements["page_indicator_1"]
        XCTAssertTrue(page2Indicator.waitForExistence(timeout: 1))
        
        continueButton.tap()
        continueButton.tap()
        
        let getStartedButton = app.buttons["Get Started"]
        XCTAssertTrue(getStartedButton.waitForExistence(timeout: 1))
        getStartedButton.tap()
        
        XCTAssertFalse(skipButton.exists)
    }
    
    func testSkipOnboarding() {
        let skipButton = app.buttons["Skip"]
        skipButton.tap()
        
        let homeView = app.otherElements["HomeView"]
        XCTAssertTrue(homeView.waitForExistence(timeout: 2))
    }
}
```

---

## 模块二：ProgressCard组件增强

### 2.1 需求描述

**用户故事：**
> 作为用户，我希望在列表中看到更多进度信息，以便快速了解观看状态

**功能目标：**
- 显示剩余时间
- 添加进度条可视化
- 优化信息层次

**验收标准：**
- [ ] 显示剩余时间
- [ ] 添加线性进度条
- [ ] 保持现有交互

### 2.2 生成规则

#### 复用现有组件

```swift
// 复用 ProgressRing 组件
// 复用 CardStyle modifier
// 复用 Constants.UI 常量
// 复用 Color+Extensions
```

#### 文件结构

```
Features/
└── Home/
    └── Components/
        ├── ProgressCard.swift          # 修改现有文件
        └── ProgressBarView.swift       # 新增组件
```

### 2.3 代码实现

#### ProgressBarView.swift（新增）

```swift
import SwiftUI

struct ProgressBarView: View {
    let progress: Double
    let color: Color
    let height: CGFloat
    
    init(
        progress: Double,
        color: Color = .accentColor,
        height: CGFloat = 4
    ) {
        self.progress = min(max(progress, 0), 100)
        self.color = color
        self.height = height
    }
    
    var body: some View {
        GeometryReader { geometry in
            ZStack(alignment: .leading) {
                RoundedRectangle(cornerRadius: height / 2)
                    .fill(Color.gray.opacity(0.2))
                    .frame(height: height)
                
                RoundedRectangle(cornerRadius: height / 2)
                    .fill(color)
                    .frame(width: geometry.size.width * (progress / 100), height: height)
                    .animation(.spring(response: 0.3), value: progress)
            }
        }
        .frame(height: height)
    }
}

#Preview {
    VStack(spacing: 20) {
        ProgressBarView(progress: 25, color: .blue)
        ProgressBarView(progress: 50, color: .green)
        ProgressBarView(progress: 75, color: .orange)
        ProgressBarView(progress: 100, color: .red)
    }
    .padding()
}
```

#### ProgressCard.swift（修改）

```swift
import SwiftUI

struct ProgressCard: View {
    let video: VideoProgress
    var onTap: (() -> Void)?

    var body: some View {
        Button(action: { onTap?() }) {
            VStack(spacing: Constants.UI.compactPadding) {
                headerSection
                Divider()
                    .padding(.vertical, 4)
                progressSection
            }
            .cardStyle()
        }
        .buttonStyle(.plain)
    }

    private var headerSection: some View {
        HStack(spacing: Constants.UI.standardPadding) {
            ProgressRing(
                progress: video.progressPercentage,
                lineWidth: 4,
                size: 50,
                color: platformColor
            )

            VStack(alignment: .leading, spacing: 6) {
                Text(video.title)
                    .font(.headline)
                    .foregroundStyle(.primary)
                    .lineLimit(2)

                HStack(spacing: 6) {
                    Image(systemName: video.streamingPlatform.iconName)
                        .font(.caption)
                        .foregroundStyle(platformColor)

                    Text(video.streamingPlatform.displayName)
                        .font(.subheadline)
                        .foregroundStyle(.secondary)
                }
            }

            Spacer()

            VStack(alignment: .trailing, spacing: 4) {
                Text(video.lastUpdated.relativeString)
                    .font(.caption2)
                    .foregroundStyle(.tertiary)

                if video.isCompleted {
                    Image(systemName: "checkmark.circle.fill")
                        .foregroundStyle(.green)
                        .font(.title3)
                }
            }
        }
    }

    private var progressSection: some View {
        HStack {
            VStack(alignment: .leading, spacing: 4) {
                Text("Watched")
                    .font(.caption2)
                    .foregroundStyle(.tertiary)
                Text(video.formattedCurrentPosition)
                    .font(.subheadline.weight(.medium))
            }

            Spacer()

            VStack(alignment: .center, spacing: 4) {
                ProgressBarView(
                    progress: video.progressPercentage,
                    color: platformColor,
                    height: 4
                )
                .frame(width: 80)
                Text("\(Int(video.progressPercentage))%")
                    .font(.caption2)
                    .foregroundStyle(.secondary)
            }

            Spacer()

            VStack(alignment: .trailing, spacing: 4) {
                Text("Remaining")
                    .font(.caption2)
                    .foregroundStyle(.tertiary)
                Text(video.remainingTime.formattedTime())
                    .font(.subheadline.weight(.medium))
            }
        }
    }

    private var platformColor: Color {
        Color(hex: video.streamingPlatform.accentColor)
    }
}
```

> **注意**：使用现有的 `CardStyle` modifier，遵循代码复用原则。`CardStyle` 已定义在 `Shared/Modifiers/CardStyle.swift` 中，包含 `.ultraThinMaterial` 背景、圆角、边框和阴影效果。

### 2.4 测试用例

```swift
import XCTest
@testable import ResumePoint

final class ProgressCardTests: XCTestCase {
    
    func testProgressBarViewProgress() {
        let view = ProgressBarView(progress: 50, color: .blue)
        XCTAssertNotNil(view.body)
    }
    
    func testProgressCardWithVideo() {
        let dependencies = DependencyContainer.preview
        let context = dependencies.persistentContainer.viewContext
        let video = VideoProgress(context: context)
        video.id = UUID()
        video.title = "Test Video"
        video.platform = StreamingPlatform.netflix.rawValue
        video.currentPosition = 3600
        video.totalDuration = 7200
        video.isCompleted = false
        video.lastUpdated = Date()
        video.createdAt = Date()
        
        let card = ProgressCard(video: video)
        XCTAssertNotNil(card.body)
    }
    
    func testProgressBarAnimation() {
        let view = ProgressBarView(progress: 75, color: .green, height: 6)
        XCTAssertNotNil(view.body)
    }
}
```

---

## 模块三：智能提醒系统

### 3.1 需求描述

**用户故事：**
> 作为用户，我希望在合适的时机收到提醒，以便继续观看未完成的视频

**功能目标：**
- 智能判断提醒时机
- 支持多种提醒规则
- 用户可配置

**验收标准：**
- [ ] 支持长时间未观看提醒
- [ ] 支持新剧集提醒（预留接口）
- [ ] 支持进度接近结尾提醒
- [ ] 用户可配置提醒规则

### 3.2 生成规则

#### 文件结构

```
Core/
└── Services/
    ├── NotificationService.swift      # 修改现有文件
    └── SmartReminderService.swift     # 新增服务

Features/
└── Settings/
    └── Components/
        └── ReminderSettingsView.swift # 新增设置组件
```

#### 继承现有协议

```swift
// 继承 NotificationServicing 协议
protocol SmartReminderServicing: NotificationServicing {
    func scheduleSmartReminders(for video: VideoProgress) async throws
    func evaluateReminderRules(for video: VideoProgress) -> [ReminderRule]
}
```

### 3.3 代码实现

#### ReminderRule.swift（新增到Models）

```swift
import Foundation

enum ReminderRule: String, CaseIterable, Identifiable, Codable {
    case longTimeNoWatch = "longTimeNoWatch"
    case nearCompletion = "nearCompletion"
    case newEpisode = "newEpisode"
    case weeklyDigest = "weeklyDigest"
    
    var id: String { rawValue }
    
    var displayName: String {
        switch self {
        case .longTimeNoWatch: return "Long Time No Watch"
        case .nearCompletion: return "Near Completion"
        case .newEpisode: return "New Episode"
        case .weeklyDigest: return "Weekly Digest"
        }
    }
    
    var description: String {
        switch self {
        case .longTimeNoWatch: return "Remind when haven't watched for 7 days"
        case .nearCompletion: return "Remind when progress > 90%"
        case .newEpisode: return "Remind when new episode available"
        case .weeklyDigest: return "Weekly watching summary"
        }
    }
    
    var icon: String {
        switch self {
        case .longTimeNoWatch: return "clock.arrow.circlepath"
        case .nearCompletion: return "flag.checkered"
        case .newEpisode: return "sparkles"
        case .weeklyDigest: return "calendar.badge.clock"
        }
    }
}

struct ReminderConfig: Codable {
    var enabledRules: Set<ReminderRule>
    var longTimeThreshold: TimeInterval
    var nearCompletionThreshold: Double
    var quietHoursStart: Int
    var quietHoursEnd: Int
    
    static let `default` = ReminderConfig(
        enabledRules: [.longTimeNoWatch, .nearCompletion],
        longTimeThreshold: 7 * 24 * 3600,
        nearCompletionThreshold: 90.0,
        quietHoursStart: 22,
        quietHoursEnd: 8
    )
}
```

#### SmartReminderService.swift

```swift
import Foundation
import UserNotifications

protocol SmartReminderServicing {
    var config: ReminderConfig { get set }
    func evaluateAndSchedule(for video: VideoProgress) async throws
    func updateConfig(_ config: ReminderConfig)
    func isQuietHour() -> Bool
}

final class SmartReminderService: SmartReminderServicing, ObservableObject {
    @Published var config: ReminderConfig = .default
    private let notificationService: NotificationServicing
    private let center = UNUserNotificationCenter.current()
    
    init(notificationService: NotificationServicing) {
        self.notificationService = notificationService
        loadConfig()
    }
    
    func evaluateAndSchedule(for video: VideoProgress) async throws {
        guard !video.isCompleted else { return }
        
        let rules = evaluateRules(for: video)
        
        for rule in rules where config.enabledRules.contains(rule) {
            try await scheduleReminder(for: video, rule: rule)
        }
    }
    
    func updateConfig(_ config: ReminderConfig) {
        self.config = config
        saveConfig()
    }
    
    func isQuietHour() -> Bool {
        let hour = Calendar.current.component(.hour, from: Date())
        return hour >= config.quietHoursStart || hour < config.quietHoursEnd
    }
    
    private func evaluateRules(for video: VideoProgress) -> [ReminderRule] {
        var triggeredRules: [ReminderRule] = []
        
        if let lastWatched = video.lastUpdated.timeIntervalSinceNow,
           abs(lastWatched) > config.longTimeThreshold {
            triggeredRules.append(.longTimeNoWatch)
        }
        
        if video.progressPercentage >= config.nearCompletionThreshold {
            triggeredRules.append(.nearCompletion)
        }
        
        return triggeredRules
    }
    
    private func scheduleReminder(for video: VideoProgress, rule: ReminderRule) async throws {
        let content = UNMutableNotificationContent()
        content.title = "Continue Watching"
        content.body = buildBody(for: video, rule: rule)
        content.sound = .default
        content.userInfo = [
            "videoId": video.id.uuidString,
            "rule": rule.rawValue
        ]
        
        let trigger: UNNotificationTrigger
        
        switch rule {
        case .longTimeNoWatch:
            let tomorrow = Calendar.current.date(
                byAdding: .day,
                value: 1,
                to: Date()
            ) ?? Date()
            let components = Calendar.current.dateComponents(
                [.year, .month, .day, .hour],
                from: tomorrow
            )
            trigger = UNCalendarNotificationTrigger(
                dateMatching: components,
                repeats: false
            )
            
        case .nearCompletion:
            let nextHour = Calendar.current.date(
                byAdding: .hour,
                value: 1,
                to: Date()
            ) ?? Date()
            let components = Calendar.current.dateComponents(
                [.year, .month, .day, .hour],
                from: nextHour
            )
            trigger = UNCalendarNotificationTrigger(
                dateMatching: components,
                repeats: false
            )
            
        default:
            return
        }
        
        let request = UNNotificationRequest(
            identifier: "reminder-\(video.id.uuidString)-\(rule.rawValue)",
            content: content,
            trigger: trigger
        )
        
        try await center.add(request)
    }
    
    private func buildBody(for video: VideoProgress, rule: ReminderRule) -> String {
        switch rule {
        case .longTimeNoWatch:
            return "You haven't watched \"\(video.title)\" in a while. Continue at \(video.formattedCurrentPosition)?"
        case .nearCompletion:
            return "Almost done! \(video.remainingTime.formattedTime()) left in \"\(video.title)\""
        default:
            return "Continue watching \"\(video.title)\""
        }
    }
    
    private func loadConfig() {
        guard let data = UserDefaults.standard.data(forKey: "reminderConfig"),
              let saved = try? JSONDecoder().decode(ReminderConfig.self, from: data) else {
            return
        }
        config = saved
    }
    
    private func saveConfig() {
        guard let data = try? JSONEncoder().encode(config) else { return }
        UserDefaults.standard.set(data, forKey: "reminderConfig")
    }
}
```

#### ReminderSettingsView.swift

```swift
import SwiftUI

struct ReminderSettingsView: View {
    @ObservedObject var reminderService: SmartReminderService
    @Environment(\.dismiss) private var dismiss
    
    var body: some View {
        NavigationStack {
            Form {
                Section("Reminder Rules") {
                    ForEach(ReminderRule.allCases) { rule in
                        Toggle(isOn: Binding(
                            get: { reminderService.config.enabledRules.contains(rule) },
                            set: { enabled in
                                if enabled {
                                    reminderService.config.enabledRules.insert(rule)
                                } else {
                                    reminderService.config.enabledRules.remove(rule)
                                }
                                reminderService.updateConfig(reminderService.config)
                            }
                        )) {
                            Label(rule.displayName, systemImage: rule.icon)
                        }
                    }
                }
                
                Section("Timing") {
                    Stepper(
                        "Long time threshold: \(Int(reminderService.config.longTimeThreshold / 86400)) days",
                        value: Binding(
                            get: { reminderService.config.longTimeThreshold / 86400 },
                            set: { reminderService.config.longTimeThreshold = $0 * 86400 }
                        ),
                        in: 1...30
                    )
                    
                    Stepper(
                        "Near completion: \(Int(reminderService.config.nearCompletionThreshold))%",
                        value: $reminderService.config.nearCompletionThreshold,
                        in: 50...99
                    )
                }
                
                Section("Quiet Hours") {
                    HStack {
                        Text("Start")
                        Spacer()
                        Text("\(reminderService.config.quietHoursStart):00")
                            .foregroundStyle(.secondary)
                    }
                    
                    HStack {
                        Text("End")
                        Spacer()
                        Text("\(reminderService.config.quietHoursEnd):00")
                            .foregroundStyle(.secondary)
                    }
                }
            }
            .navigationTitle("Smart Reminders")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button("Done") { dismiss() }
                }
            }
        }
    }
}
```

### 3.4 集成到设置页面

#### 修改 DependencyContainer.swift

```swift
import SwiftUI
import CoreData

@MainActor
final class DependencyContainer: ObservableObject {
    let persistentContainer: NSPersistentContainer
    let progressRepository: ProgressRepositoryProtocol
    let sessionRepository: SessionRepositoryProtocol
    let playbackMonitor: PlaybackMonitorService
    let storageService: ProgressStorageService
    let notificationService: NotificationServicing
    let reminderService: SmartReminderService  // 新增
    
    init() {
        let model = CoreDataStack.createModel()
        let container = NSPersistentContainer(name: "ResumePointDataModel", managedObjectModel: model)
        container.loadPersistentStores { _, error in
            if let error = error {
                fatalError("CoreData failed: \(error.localizedDescription)")
            }
        }
        container.viewContext.automaticallyMergesChangesFromParent = true
        container.viewContext.mergePolicy = NSMergeByPropertyObjectTrumpMergePolicy
        self.persistentContainer = container

        self.progressRepository = ProgressRepositoryImpl(container: container)
        self.sessionRepository = SessionRepositoryImpl(container: container)
        self.playbackMonitor = PlaybackMonitorService()
        self.notificationService = NotificationService()
        
        // 新增：初始化智能提醒服务
        self.reminderService = SmartReminderService(notificationService: notificationService)
        
        self.storageService = ProgressStorageService(
            repository: progressRepository,
            playbackMonitor: playbackMonitor
        )
    }

    static var preview: DependencyContainer {
        let container = DependencyContainer()
        let context = container.persistentContainer.viewContext
        for i in 1...5 {
            let video = VideoProgress(context: context)
            video.id = UUID()
            video.title = "Sample Video \(i)"
            video.platform = StreamingPlatform.allCases[i % StreamingPlatform.allCases.count].rawValue
            video.currentPosition = Double.random(in: 300...3600)
            video.totalDuration = 7200
            video.lastUpdated = Date().addingTimeInterval(-Double.random(in: 0...86400))
            video.isCompleted = false
            video.createdAt = Date().addingTimeInterval(-Double.random(in: 86400...604800))
        }
        try? context.save()
        return container
    }
}
```

#### 修改 SettingsViewModel.swift

```swift
import SwiftUI

@MainActor
final class SettingsViewModel: ObservableObject {
    @Published var isMonitoringEnabled = true
    @Published var isNotificationsEnabled = false
    @Published var selectedAppearance: AppearanceMode = .system
    @Published var videoCount: Int = 0
    @Published var isExporting = false

    private let storageService: ProgressStoring
    private let notificationService: NotificationServicing
    private let playbackMonitor: PlaybackMonitorService
    let reminderService: SmartReminderService  // 新增：公开给视图使用

    enum AppearanceMode: String, CaseIterable {
        case system = "system"
        case light = "light"
        case dark = "dark"

        var displayName: String {
            switch self {
            case .system: return "System"
            case .light: return "Light"
            case .dark: return "Dark"
            }
        }
    }

    init(
        storageService: ProgressStoring,
        notificationService: NotificationServicing,
        playbackMonitor: PlaybackMonitorService,
        reminderService: SmartReminderService  // 新增参数
    ) {
        self.storageService = storageService
        self.notificationService = notificationService
        self.playbackMonitor = playbackMonitor
        self.reminderService = reminderService
        loadPreferences()
    }

    func toggleMonitoring() {
        if isMonitoringEnabled {
            playbackMonitor.startMonitoring()
        } else {
            playbackMonitor.stopMonitoring()
        }
        savePreference(key: "isMonitoringEnabled", value: isMonitoringEnabled)
    }

    func toggleNotifications() async {
        if isNotificationsEnabled {
            let granted = try? await notificationService.requestPermission()
            isNotificationsEnabled = granted ?? false
        } else {
            notificationService.removeAllPendingNotifications()
        }
        savePreference(key: "isNotificationsEnabled", value: isNotificationsEnabled)
    }

    func loadStats() async {
        videoCount = (try? await storageService.videoCount()) ?? 0
    }

    func exportData() async -> Data? {
        isExporting = true
        let data = try? await storageService.exportToJSON()
        isExporting = false
        return data
    }

    private func loadPreferences() {
        isMonitoringEnabled = UserDefaults.standard.bool(forKey: "isMonitoringEnabled")
        isNotificationsEnabled = UserDefaults.standard.bool(forKey: "isNotificationsEnabled")
        if let appearanceRaw = UserDefaults.standard.string(forKey: "selectedAppearance"),
           let appearance = AppearanceMode(rawValue: appearanceRaw) {
            selectedAppearance = appearance
        }
    }

    private func savePreference(key: String, value: Any) {
        UserDefaults.standard.set(value, forKey: key)
    }
}
```

#### 修改 SettingsView.swift

```swift
struct SettingsView: View {
    @StateObject private var viewModel: SettingsViewModel
    @State private var showingExportSheet = false
    @State private var exportData: Data?

    init(
        storageService: ProgressStoring,
        notificationService: NotificationServicing,
        playbackMonitor: PlaybackMonitorService,
        reminderService: SmartReminderService  // 新增参数
    ) {
        _viewModel = StateObject(wrappedValue: SettingsViewModel(
            storageService: storageService,
            notificationService: notificationService,
            playbackMonitor: playbackMonitor,
            reminderService: reminderService  // 传递参数
        ))
    }

    // ... 其他代码保持不变 ...

    private var notificationsSection: some View {
        Section("Notifications") {
            Toggle("Enable Reminders", isOn: $viewModel.isNotificationsEnabled)
                .onChange(of: viewModel.isNotificationsEnabled) { _, _ in
                    Task { await viewModel.toggleNotifications() }
                }
            
            if viewModel.isNotificationsEnabled {
                NavigationLink {
                    ReminderSettingsView(reminderService: viewModel.reminderService)
                } label: {
                    Label("Smart Reminders", systemImage: "bell.badge")
                }
            }
        }
    }
}
```

#### 修改 ContentView.swift

```swift
import SwiftUI

struct ContentView: View {
    @ObservedObject var dependencies: DependencyContainer
    @State private var selectedTab = 0

    var body: some View {
        TabView(selection: $selectedTab) {
            HomeView(storageService: dependencies.storageService)
                .tabItem {
                    Label("Home", systemImage: "house.fill")
                }
                .tag(0)

            PlatformsView(storageService: dependencies.storageService)
                .tabItem {
                    Label("Platforms", systemImage: "tv.fill")
                }
                .tag(1)

            SearchView(storageService: dependencies.storageService)
                .tabItem {
                    Label("Search", systemImage: "magnifyingglass")
                }
                .tag(2)

            SettingsView(
                storageService: dependencies.storageService,
                notificationService: dependencies.notificationService,
                playbackMonitor: dependencies.playbackMonitor,
                reminderService: dependencies.reminderService  // 新增参数
            )
            .tabItem {
                Label("Settings", systemImage: "gearshape.fill")
            }
            .tag(3)
        }
        .tint(.accentColor)
    }
}
```

### 3.5 测试用例

```swift
import XCTest
@testable import ResumePoint

final class SmartReminderServiceTests: XCTestCase {
    
    var sut: SmartReminderService!
    var mockNotificationService: MockNotificationService!
    
    override func setUp() {
        mockNotificationService = MockNotificationService()
        sut = SmartReminderService(notificationService: mockNotificationService)
    }
    
    func testDefaultConfig() {
        XCTAssertTrue(sut.config.enabledRules.contains(.longTimeNoWatch))
        XCTAssertTrue(sut.config.enabledRules.contains(.nearCompletion))
        XCTAssertEqual(sut.config.longTimeThreshold, 7 * 24 * 3600)
    }
    
    func testQuietHourDetection() {
        sut.config.quietHoursStart = 22
        sut.config.quietHoursEnd = 8
        
        let isQuiet = sut.isQuietHour()
        let hour = Calendar.current.component(.hour, from: Date())
        
        if hour >= 22 || hour < 8 {
            XCTAssertTrue(isQuiet)
        } else {
            XCTAssertFalse(isQuiet)
        }
    }
    
    func testUpdateConfig() {
        var newConfig = ReminderConfig.default
        newConfig.longTimeThreshold = 14 * 24 * 3600
        
        sut.updateConfig(newConfig)
        
        XCTAssertEqual(sut.config.longTimeThreshold, 14 * 24 * 3600)
    }
}

class MockNotificationService: NotificationServicing {
    func requestPermission() async throws -> Bool { true }
    func scheduleReminder(for video: VideoProgress, at date: Date) async throws {}
    func cancelReminder(for video: VideoProgress) async throws {}
    func removeAllPendingNotifications() {}
}
```

---

## 模块四：平台图标系统

### 4.1 需求描述

**用户故事：**
> 作为用户，我希望看到熟悉的流媒体平台图标，以便快速识别

**功能目标：**
- 使用平台官方颜色
- 支持自定义图标
- 保持SF Symbols备选

**验收标准：**
- [ ] 显示平台品牌色
- [ ] 支持深色模式
- [ ] 保持一致性

### 4.2 生成规则

#### 复用现有代码

```swift
// 复用 Platform.swift 中的 accentColor
// 复用 Color+Extensions.swift 中的 Color(hex:)
// 复用 Constants.UI 常量
```

#### 文件结构

```
Shared/
└── Components/
    └── PlatformIconView.swift  # 新增组件
```

### 4.3 代码实现

#### PlatformIconView.swift

```swift
import SwiftUI

struct PlatformIconView: View {
    let platform: StreamingPlatform
    var size: CGFloat = 40
    
    var body: some View {
        ZStack {
            Circle()
                .fill(platformColor.opacity(0.15))
            
            Image(systemName: platform.iconName)
                .font(.system(size: size * 0.5, weight: .semibold))
                .foregroundStyle(platformColor)
        }
        .frame(width: size, height: size)
    }
    
    private var platformColor: Color {
        Color(hex: platform.accentColor)
    }
}

#Preview {
    HStack(spacing: 16) {
        PlatformIconView(platform: .netflix)
        PlatformIconView(platform: .disneyplus)
        PlatformIconView(platform: .hbomax)
        PlatformIconView(platform: .primevideo)
        PlatformIconView(platform: .hulu)
        PlatformIconView(platform: .youtube)
    }
    .padding()
}
```

#### 修改 ProgressCard.swift

```swift
// 替换现有的图标显示
HStack(spacing: 6) {
    PlatformIconView(platform: video.streamingPlatform, size: 24)
    
    Text(video.streamingPlatform.displayName)
        .font(.subheadline)
        .foregroundStyle(.secondary)
}
```

---

## 模块五：数据导出增强

### 5.1 需求描述

**用户故事：**
> 作为用户，我希望导出多种格式的数据，以便备份或迁移

**功能目标：**
- 支持CSV格式
- 支持PDF格式
- 保持JSON格式

**验收标准：**
- [ ] 导出CSV
- [ ] 导出PDF
- [ ] 格式选择器

### 5.2 生成规则

#### 文件结构

```
Core/
└── Services/
    └── ExportService.swift  # 新增服务

Features/
└── Settings/
    └── Components/
        └── ExportFormatPicker.swift  # 新增组件
```

### 5.3 代码实现

#### ExportFormat.swift

```swift
import Foundation

enum ExportFormat: String, CaseIterable, Identifiable {
    case json = "json"
    case csv = "csv"
    case pdf = "pdf"
    
    var id: String { rawValue }
    
    var displayName: String {
        switch self {
        case .json: return "JSON"
        case .csv: return "CSV"
        case .pdf: return "PDF"
        }
    }
    
    var fileExtension: String {
        rawValue
    }
    
    var icon: String {
        switch self {
        case .json: return "curlybraces"
        case .csv: return "tablecells"
        case .pdf: return "doc.richtext"
        }
    }
}
```

#### ExportService.swift

```swift
import Foundation
import PDFKit

protocol ExportServicing {
    func export(videos: [VideoProgress], format: ExportFormat) async throws -> Data
    func generateCSV(from videos: [VideoProgress]) -> Data
    func generatePDF(from videos: [VideoProgress]) -> Data
}

final class ExportService: ExportServicing {
    
    func export(videos: [VideoProgress], format: ExportFormat) async throws -> Data {
        switch format {
        case .json:
            return try await exportToJSON(videos: videos)
        case .csv:
            return generateCSV(from: videos)
        case .pdf:
            return generatePDF(from: videos)
        }
    }
    
    func generateCSV(from videos: [VideoProgress]) -> Data {
        var csv = "Title,Platform,Current Position,Total Duration,Progress %,Last Updated,Completed\n"
        
        for video in videos {
            let row = [
                video.title.csvEscaped,
                video.streamingPlatform.displayName,
                video.formattedCurrentPosition,
                video.formattedTotalDuration,
                "\(Int(video.progressPercentage))%",
                video.lastUpdated.dateTimeString,
                video.isCompleted ? "Yes" : "No"
            ].joined(separator: ",")
            csv += row + "\n"
        }
        
        return Data(csv.utf8)
    }
    
    func generatePDF(from videos: [VideoProgress]) -> Data {
        let pdfData = NSMutableData()
        
        UIGraphicsBeginPDFContextToData(pdfData, .zero, nil)
        UIGraphicsBeginPDFPage()
        
        let titleFont = UIFont.systemFont(ofSize: 24, weight: .bold)
        let headerFont = UIFont.systemFont(ofSize: 14, weight: .semibold)
        let bodyFont = UIFont.systemFont(ofSize: 12)
        
        var y: CGFloat = 50
        
        "ResumePoint Export".draw(at: CGPoint(x: 50, y: y), withAttributes: [.font: titleFont])
        y += 40
        
        let dateFormatter = DateFormatter()
        dateFormatter.dateStyle = .medium
        
        "Exported: \(dateFormatter.string(from: Date()))".draw(
            at: CGPoint(x: 50, y: y),
            withAttributes: [.font: bodyFont, .foregroundColor: UIColor.secondaryLabel]
        )
        y += 30
        
        for video in videos {
            if y > 700 {
                UIGraphicsBeginPDFPage()
                y = 50
            }
            
            video.title.draw(at: CGPoint(x: 50, y: y), withAttributes: [.font: headerFont])
            y += 20
            
            let details = "\(video.streamingPlatform.displayName) • \(video.formattedCurrentPosition) / \(video.formattedTotalDuration) • \(Int(video.progressPercentage))%"
            details.draw(at: CGPoint(x: 50, y: y), withAttributes: [.font: bodyFont, .foregroundColor: UIColor.secondaryLabel])
            y += 25
        }
        
        UIGraphicsEndPDFContext()
        
        return pdfData as Data
    }
    
    private func exportToJSON(videos: [VideoProgress]) async throws -> Data {
        let exportData = videos.map { video -> [String: Any] in
            return [
                "id": video.id.uuidString,
                "title": video.title,
                "platform": video.platform,
                "currentPosition": video.currentPosition,
                "totalDuration": video.totalDuration,
                "progressPercentage": video.progressPercentage,
                "lastUpdated": video.lastUpdated.iso8601String,
                "isCompleted": video.isCompleted,
                "notes": video.notes ?? ""
            ]
        }
        return try JSONSerialization.data(withJSONObject: exportData, options: .prettyPrinted)
    }
}

extension String {
    var csvEscaped: String {
        if contains(",") || contains("\"") || contains("\n") {
            return "\"" + replacingOccurrences(of: "\"", with: "\"\"") + "\""
        }
        return self
    }
}
```

### 5.4 集成到现有代码

#### 修改 ProgressStorageService.swift

```swift
import Foundation
import CoreData
import Combine

protocol ProgressStoring {
    var videos: AnyPublisher<[VideoProgress], Never> { get }
    func fetchAll() async throws -> [VideoProgress]
    func addVideo(title: String, platform: StreamingPlatform, currentPosition: Double, totalDuration: Double, notes: String?) async throws -> VideoProgress
    func updateVideo(_ video: VideoProgress, currentPosition: Double) async throws
    func updateVideo(_ video: VideoProgress, notes: String?) async throws
    func markCompleted(_ video: VideoProgress) async throws
    func deleteVideo(_ video: VideoProgress) async throws
    func searchVideos(query: String) async throws -> [VideoProgress]
    func fetchByPlatform(_ platform: StreamingPlatform) async throws -> [VideoProgress]
    func videoCount() async throws -> Int
    func exportToJSON() async throws -> Data
    func exportToCSV() async throws -> Data      // 新增
    func exportToPDF() async throws -> Data      // 新增
    func export(format: ExportFormat) async throws -> Data  // 新增
}

final class ProgressStorageService: ProgressStoring, ObservableObject {
    // ... 现有代码 ...
    
    private let exportService: ExportService  // 新增
    
    init(repository: ProgressRepositoryProtocol, playbackMonitor: PlaybackMonitoring) {
        self.repository = repository
        self.exportService = ExportService()  // 新增：初始化导出服务
        
        // ... 现有代码 ...
    }
    
    // ... 现有代码 ...
    
    // 新增：CSV导出
    func exportToCSV() async throws -> Data {
        let allVideos = try await repository.fetchAll()
        return exportService.generateCSV(from: allVideos)
    }
    
    // 新增：PDF导出
    func exportToPDF() async throws -> Data {
        let allVideos = try await repository.fetchAll()
        return exportService.generatePDF(from: allVideos)
    }
    
    // 新增：统一导出接口
    func export(format: ExportFormat) async throws -> Data {
        let allVideos = try await repository.fetchAll()
        return try await exportService.export(videos: allVideos, format: format)
    }
}
```

#### 修改 SettingsViewModel.swift（添加导出功能）

```swift
@MainActor
final class SettingsViewModel: ObservableObject {
    // ... 现有代码 ...
    
    @Published var selectedExportFormat: ExportFormat = .json  // 新增
    
    // ... 现有代码 ...
    
    // 修改：支持多格式导出
    func exportData(format: ExportFormat) async -> Data? {
        isExporting = true
        let data = try? await storageService.export(format: format)
        isExporting = false
        return data
    }
}
```

#### 新增 ExportFormatPicker.swift

```swift
import SwiftUI

struct ExportFormatPicker: View {
    @Binding var selectedFormat: ExportFormat
    @Environment(\.dismiss) private var dismiss
    var onExport: (ExportFormat) -> Void
    
    var body: some View {
        NavigationStack {
            List {
                ForEach(ExportFormat.allCases) { format in
                    Button(action: {
                        selectedFormat = format
                        onExport(format)
                        dismiss()
                    }) {
                        HStack {
                            Label(format.displayName, systemImage: format.icon)
                            
                            Spacer()
                            
                            if selectedFormat == format {
                                Image(systemName: "checkmark")
                                    .foregroundStyle(.accentColor)
                            }
                        }
                    }
                    .buttonStyle(.plain)
                }
            }
            .navigationTitle("Export Format")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button("Cancel") { dismiss() }
                }
            }
        }
    }
}
```

#### 修改 SettingsView.swift（添加导出选择器）

```swift
private var dataSection: some View {
    Section("Data") {
        HStack {
            Text("Tracked Videos")
            Spacer()
            Text("\(viewModel.videoCount)")
                .foregroundStyle(.secondary)
        }

        Button(action: {
            Task {
                exportData = await viewModel.exportData(format: viewModel.selectedExportFormat)
                if exportData != nil {
                    showingExportSheet = true
                }
            }
        }) {
            HStack {
                Image(systemName: "square.and.arrow.up")
                Text("Export Data")
            }
        }
        .disabled(viewModel.isExporting)
        
        // 新增：格式选择
        Picker("Export Format", selection: $viewModel.selectedExportFormat) {
            ForEach(ExportFormat.allCases) { format in
                Text(format.displayName).tag(format)
            }
        }
    }
}
```

---

## 模块六：设置页面优化

### 6.1 需求描述

**用户故事：**
> 作为用户，我希望在设置中看到Pro功能，以便了解升级选项

**功能目标：**
- 添加Pro功能入口
- 显示当前状态
- 支持订阅管理

**验收标准：**
- [ ] Pro功能区域
- [ ] 订阅状态显示
- [ ] 升级入口

### 6.2 代码实现

#### ProFeature.swift

```swift
import Foundation

enum ProFeature: String, CaseIterable, Identifiable {
    case iCloudSync = "icloudSync"
    case unlimitedVideos = "unlimitedVideos"
    case smartReminders = "smartReminders"
    case exportFormats = "exportFormats"
    case widgets = "widgets"
    
    var id: String { rawValue }
    
    var displayName: String {
        switch self {
        case .iCloudSync: return "iCloud Sync"
        case .unlimitedVideos: return "Unlimited Videos"
        case .smartReminders: return "Smart Reminders"
        case .exportFormats: return "All Export Formats"
        case .widgets: return "Home Screen Widgets"
        }
    }
    
    var icon: String {
        switch self {
        case .iCloudSync: return "icloud.fill"
        case .unlimitedVideos: return "infinity"
        case .smartReminders: return "bell.badge.fill"
        case .exportFormats: return "square.and.arrow.up.fill"
        case .widgets: return "square.grid.2x2.fill"
        }
    }
    
    var description: String {
        switch self {
        case .iCloudSync: return "Sync across all your devices"
        case .unlimitedVideos: return "No limit on tracked videos"
        case .smartReminders: return "Intelligent watching reminders"
        case .exportFormats: return "CSV, PDF, and JSON export"
        case .widgets: return "Quick access from home screen"
        }
    }
}
```

#### ProSectionView.swift

```swift
import SwiftUI

struct ProSectionView: View {
    @State private var showingPaywall = false
    
    var body: some View {
        Section {
            VStack(alignment: .leading, spacing: 12) {
                HStack {
                    Image(systemName: "crown.fill")
                        .foregroundStyle(.yellow)
                    Text("ResumePoint Pro")
                        .font(.headline)
                    Spacer()
                    Button("Upgrade") {
                        showingPaywall = true
                    }
                    .buttonStyle(.borderedProminent)
                    .tint(.accentColor)
                }
                
                Text("Unlock all features")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
                
                ForEach(ProFeature.allCases.prefix(3)) { feature in
                    HStack(spacing: 8) {
                        Image(systemName: feature.icon)
                            .font(.caption)
                            .foregroundStyle(.green)
                        Text(feature.displayName)
                            .font(.subheadline)
                    }
                }
            }
            .padding(.vertical, 8)
        } header: {
            Text("Pro Features")
        }
        .sheet(isPresented: $showingPaywall) {
            PaywallView()
        }
    }
}
```

---

## 代码清理计划

### 清理流程

```
Phase 1: 标记废弃
├── 添加 @available(*, deprecated) 注解
├── 添加替代方案注释
└── 更新文档

Phase 2: 验证无影响
├── 运行单元测试
├── 运行UI测试
├── 手动测试
└── 代码审查

Phase 3: 删除代码
├── 删除废弃代码
├── 删除相关注释
├── 更新导入语句
└── 提交清理
```

### 清理清单

| 文件 | 清理内容 | 原因 | 状态 |
|------|---------|------|------|
| ProgressCard.swift | 旧版卡片样式 | 被新版本替代 | 待处理 |
| SettingsView.swift | 简单提醒设置 | 被智能提醒替代 | 待处理 |
| Constants.swift | 重复常量 | 合并到统一位置 | 待处理 |

---

## 测试验证标准

### 单元测试覆盖

| 模块 | 目标覆盖率 | 当前覆盖率 |
|------|-----------|-----------|
| OnboardingViewModel | 90% | - |
| SmartReminderService | 85% | - |
| ExportService | 80% | - |
| ProgressStorageService | 85% | - |

### UI测试场景

```gherkin
Feature: Onboarding Flow
  
  Scenario: Complete onboarding
    Given app is launched for the first time
    When I see onboarding screen
    And I tap "Continue" 4 times
    Then I should see "Get Started" button
    And I tap "Get Started"
    Then onboarding should not appear again

  Scenario: Skip onboarding
    Given app is launched for the first time
    When I tap "Skip"
    Then I should see home screen
    And onboarding should not appear again

Feature: Smart Reminders
  
  Scenario: Enable smart reminders
    Given I am on settings screen
    When I enable "Enable Reminders"
    Then I should see "Smart Reminders" option
    When I tap "Smart Reminders"
    Then I should see reminder rules configuration
```

### 性能测试

```swift
func testProgressCardPerformance() {
    let videos = (0..<100).map { _ in
        VideoProgress.mock()
    }
    
    measure {
        for video in videos {
            _ = ProgressCard(video: video)
        }
    }
}
```

---

## 实施时间表

### Week 1: 核心功能

| 日期 | 任务 | 预计时间 |
|------|------|---------|
| Day 1-2 | Onboarding模块 | 4h |
| Day 3-4 | ProgressCard增强 | 3h |
| Day 5 | 单元测试 | 2h |

### Week 2: 增强功能

| 日期 | 任务 | 预计时间 |
|------|------|---------|
| Day 1-2 | 智能提醒系统 | 4h |
| Day 3 | 平台图标系统 | 2h |
| Day 4 | 数据导出增强 | 3h |
| Day 5 | 集成测试 | 2h |

### Week 3: 优化与发布

| 日期 | 任务 | 预计时间 |
|------|------|---------|
| Day 1-2 | 设置页面优化 | 3h |
| Day 3 | 代码清理 | 2h |
| Day 4 | 文档更新 | 2h |
| Day 5 | 发布准备 | 2h |

---

## 附录

### A. 文件结构总览

```
ResumePoint/
├── App/
│   ├── ResumePointApp.swift          # 修改：添加Onboarding
│   └── DependencyContainer.swift     # 修改：添加新服务
├── Core/
│   ├── Models/
│   │   ├── VideoProgress.swift       # 保持
│   │   ├── Platform.swift            # 保持
│   │   ├── PlaybackInfo.swift        # 保持
│   │   ├── WatchingSession.swift     # 保持
│   │   ├── ReminderRule.swift        # 新增
│   │   ├── ReminderConfig.swift      # 新增
│   │   ├── ExportFormat.swift        # 新增
│   │   └── ProFeature.swift          # 新增
│   ├── Services/
│   │   ├── PlaybackMonitorService.swift
│   │   ├── ProgressStorageService.swift
│   │   ├── NotificationService.swift # 修改：扩展协议
│   │   ├── SmartReminderService.swift # 新增
│   │   └── ExportService.swift       # 新增
│   ├── Repositories/
│   │   ├── ProgressRepository.swift
│   │   └── SessionRepository.swift
│   └── Extensions/
│       ├── Date+Extensions.swift
│       ├── Double+Extensions.swift
│       ├── Color+Extensions.swift
│       └── String+Extensions.swift   # 新增：csvEscaped
├── Features/
│   ├── Home/
│   │   ├── HomeView.swift
│   │   ├── HomeViewModel.swift
│   │   └── Components/
│   │       ├── ProgressCard.swift    # 修改：增强显示
│   │       ├── ProgressList.swift
│   │       └── ProgressBarView.swift # 新增
│   ├── Onboarding/                   # 新增目录
│   │   ├── OnboardingView.swift
│   │   ├── OnboardingViewModel.swift
│   │   ├── OnboardingPage.swift
│   │   └── Components/
│   │       ├── OnboardingPageView.swift
│   │       └── OnboardingProgressDots.swift
│   ├── Platforms/
│   │   ├── PlatformsView.swift
│   │   └── PlatformsViewModel.swift
│   ├── Search/
│   │   ├── SearchView.swift
│   │   └── SearchViewModel.swift
│   ├── Detail/
│   │   ├── DetailView.swift
│   │   └── DetailViewModel.swift
│   ├── Add/
│   │   ├── AddProgressView.swift
│   │   └── AddProgressViewModel.swift
│   └── Settings/
│       ├── SettingsView.swift        # 修改：添加Pro入口
│       ├── SettingsViewModel.swift   # 修改：添加新服务
│       └── Components/               # 新增目录
│           ├── ReminderSettingsView.swift
│           ├── ExportFormatPicker.swift
│           └── ProSectionView.swift
├── Shared/
│   ├── Components/
│   │   ├── AsyncImageView.swift
│   │   ├── EmptyStateView.swift
│   │   ├── ProgressRing.swift
│   │   └── PlatformIconView.swift    # 新增
│   ├── Modifiers/
│   │   ├── CardStyle.swift
│   │   └── ShimmerEffect.swift
│   └── Utilities/
│       ├── Constants.swift           # 修改：添加新常量
│       └── TimeFormatter.swift
└── Tests/
    ├── UnitTests/
    │   ├── Services/
    │   │   ├── OnboardingViewModelTests.swift  # 新增
    │   │   ├── SmartReminderServiceTests.swift # 新增
    │   │   └── ExportServiceTests.swift        # 新增
    │   └── Components/
    │       └── ProgressCardTests.swift         # 新增
    └── UITests/
        ├── OnboardingUITests.swift             # 新增
        └── SettingsUITests.swift               # 新增
```

### B. Git提交规范

```
feat: 新功能
fix: 修复bug
refactor: 重构
docs: 文档更新
test: 测试相关
chore: 构建/工具相关

示例：
feat(onboarding): add onboarding flow for new users
fix(progress-card): fix progress bar animation
refactor(settings): extract reminder settings to separate view
test(onboarding): add unit tests for OnboardingViewModel
```

### C. 修改文件清单

| 文件 | 修改类型 | 修改内容 |
|------|---------|---------|
| ResumePointApp.swift | 修改 | 添加Onboarding判断逻辑 |
| DependencyContainer.swift | 修改 | 添加SmartReminderService和ExportService初始化 |
| ProgressStorageService.swift | 修改 | 添加export(format:)方法 |
| ProgressCard.swift | 修改 | 使用CardStyle modifier，拆分为headerSection和progressSection |
| SettingsView.swift | 修改 | 添加Pro入口、导出格式选择、reminderService参数 |
| SettingsViewModel.swift | 修改 | 添加reminderService属性、selectedExportFormat属性、exportData方法 |
| ContentView.swift | 修改 | 传递reminderService到SettingsView |

### D. 新增文件清单

| 文件 | 所属模块 | 说明 |
|------|---------|------|
| OnboardingView.swift | Onboarding | 主引导视图 |
| OnboardingViewModel.swift | Onboarding | 引导视图模型 |
| OnboardingPage.swift | Onboarding | 引导页数据模型 |
| OnboardingPageView.swift | Onboarding | 单个引导页视图 |
| OnboardingProgressDots.swift | Onboarding | 进度指示器组件 |
| ReminderRule.swift | Core/Models | 提醒规则枚举 |
| ReminderConfig.swift | Core/Models | 提醒配置结构体 |
| ExportFormat.swift | Core/Models | 导出格式枚举 |
| ProFeature.swift | Core/Models | Pro功能枚举 |
| SmartReminderService.swift | Core/Services | 智能提醒服务 |
| ExportService.swift | Core/Services | 导出服务 |
| ProgressBarView.swift | Features/Home | 进度条视图组件 |
| PlatformIconView.swift | Shared/Components | 平台图标组件 |
| ReminderSettingsView.swift | Features/Settings | 提醒设置视图 |
| ExportFormatPicker.swift | Features/Settings | 导出格式选择器 |
| ProSectionView.swift | Features/Settings | Pro功能区域 |
| String+Extensions.swift | Core/Extensions | 添加csvEscaped扩展 |

### E. 代码审查清单

- [ ] 遵循Swift命名规范
- [ ] 使用现有Constants常量
- [ ] 复用现有组件
- [ ] 添加必要注释
- [ ] 单元测试覆盖
- [ ] UI测试覆盖
- [ ] 性能测试通过
- [ ] 无内存泄漏
- [ ] 支持深色模式
- [ ] 支持Dynamic Type
- [ ] VoiceOver兼容

---

**文档版本**: 1.1  
**创建日期**: 2026-04-16  
**最后更新**: 2026-04-17  
**作者**: AI Assistant

**修订记录**:
- v1.1 (2026-04-17): 修复问题清单中的所有问题，添加完整的集成说明
- v1.0 (2026-04-16): 初始版本
